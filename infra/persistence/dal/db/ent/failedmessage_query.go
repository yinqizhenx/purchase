// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"purchase/infra/persistence/dal/db/ent/failedmessage"
	"purchase/infra/persistence/dal/db/ent/predicate"

	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// FailedMessageQuery is the builder for querying FailedMessage entities.
type FailedMessageQuery struct {
	config
	ctx        *QueryContext
	order      []failedmessage.OrderOption
	inters     []Interceptor
	predicates []predicate.FailedMessage
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FailedMessageQuery builder.
func (fmq *FailedMessageQuery) Where(ps ...predicate.FailedMessage) *FailedMessageQuery {
	fmq.predicates = append(fmq.predicates, ps...)
	return fmq
}

// Limit the number of records to be returned by this query.
func (fmq *FailedMessageQuery) Limit(limit int) *FailedMessageQuery {
	fmq.ctx.Limit = &limit
	return fmq
}

// Offset to start from.
func (fmq *FailedMessageQuery) Offset(offset int) *FailedMessageQuery {
	fmq.ctx.Offset = &offset
	return fmq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fmq *FailedMessageQuery) Unique(unique bool) *FailedMessageQuery {
	fmq.ctx.Unique = &unique
	return fmq
}

// Order specifies how the records should be ordered.
func (fmq *FailedMessageQuery) Order(o ...failedmessage.OrderOption) *FailedMessageQuery {
	fmq.order = append(fmq.order, o...)
	return fmq
}

// First returns the first FailedMessage entity from the query.
// Returns a *NotFoundError when no FailedMessage was found.
func (fmq *FailedMessageQuery) First(ctx context.Context) (*FailedMessage, error) {
	nodes, err := fmq.Limit(1).All(setContextOp(ctx, fmq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{failedmessage.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fmq *FailedMessageQuery) FirstX(ctx context.Context) *FailedMessage {
	node, err := fmq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FailedMessage ID from the query.
// Returns a *NotFoundError when no FailedMessage ID was found.
func (fmq *FailedMessageQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fmq.Limit(1).IDs(setContextOp(ctx, fmq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{failedmessage.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fmq *FailedMessageQuery) FirstIDX(ctx context.Context) int64 {
	id, err := fmq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FailedMessage entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FailedMessage entity is found.
// Returns a *NotFoundError when no FailedMessage entities are found.
func (fmq *FailedMessageQuery) Only(ctx context.Context) (*FailedMessage, error) {
	nodes, err := fmq.Limit(2).All(setContextOp(ctx, fmq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{failedmessage.Label}
	default:
		return nil, &NotSingularError{failedmessage.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fmq *FailedMessageQuery) OnlyX(ctx context.Context) *FailedMessage {
	node, err := fmq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FailedMessage ID in the query.
// Returns a *NotSingularError when more than one FailedMessage ID is found.
// Returns a *NotFoundError when no entities are found.
func (fmq *FailedMessageQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = fmq.Limit(2).IDs(setContextOp(ctx, fmq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{failedmessage.Label}
	default:
		err = &NotSingularError{failedmessage.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fmq *FailedMessageQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := fmq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FailedMessages.
func (fmq *FailedMessageQuery) All(ctx context.Context) ([]*FailedMessage, error) {
	ctx = setContextOp(ctx, fmq.ctx, "All")
	if err := fmq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FailedMessage, *FailedMessageQuery]()
	return withInterceptors[[]*FailedMessage](ctx, fmq, qr, fmq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fmq *FailedMessageQuery) AllX(ctx context.Context) []*FailedMessage {
	nodes, err := fmq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FailedMessage IDs.
func (fmq *FailedMessageQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if fmq.ctx.Unique == nil && fmq.path != nil {
		fmq.Unique(true)
	}
	ctx = setContextOp(ctx, fmq.ctx, "IDs")
	if err = fmq.Select(failedmessage.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fmq *FailedMessageQuery) IDsX(ctx context.Context) []int64 {
	ids, err := fmq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fmq *FailedMessageQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fmq.ctx, "Count")
	if err := fmq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fmq, querierCount[*FailedMessageQuery](), fmq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fmq *FailedMessageQuery) CountX(ctx context.Context) int {
	count, err := fmq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fmq *FailedMessageQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fmq.ctx, "Exist")
	switch _, err := fmq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fmq *FailedMessageQuery) ExistX(ctx context.Context) bool {
	exist, err := fmq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FailedMessageQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fmq *FailedMessageQuery) Clone() *FailedMessageQuery {
	if fmq == nil {
		return nil
	}
	return &FailedMessageQuery{
		config:     fmq.config,
		ctx:        fmq.ctx.Clone(),
		order:      append([]failedmessage.OrderOption{}, fmq.order...),
		inters:     append([]Interceptor{}, fmq.inters...),
		predicates: append([]predicate.FailedMessage{}, fmq.predicates...),
		// clone intermediate query.
		sql:  fmq.sql.Clone(),
		path: fmq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MessageID string `json:"message_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FailedMessage.Query().
//		GroupBy(failedmessage.FieldMessageID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fmq *FailedMessageQuery) GroupBy(field string, fields ...string) *FailedMessageGroupBy {
	fmq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FailedMessageGroupBy{build: fmq}
	grbuild.flds = &fmq.ctx.Fields
	grbuild.label = failedmessage.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MessageID string `json:"message_id,omitempty"`
//	}
//
//	client.FailedMessage.Query().
//		Select(failedmessage.FieldMessageID).
//		Scan(ctx, &v)
func (fmq *FailedMessageQuery) Select(fields ...string) *FailedMessageSelect {
	fmq.ctx.Fields = append(fmq.ctx.Fields, fields...)
	sbuild := &FailedMessageSelect{FailedMessageQuery: fmq}
	sbuild.label = failedmessage.Label
	sbuild.flds, sbuild.scan = &fmq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FailedMessageSelect configured with the given aggregations.
func (fmq *FailedMessageQuery) Aggregate(fns ...AggregateFunc) *FailedMessageSelect {
	return fmq.Select().Aggregate(fns...)
}

func (fmq *FailedMessageQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fmq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fmq); err != nil {
				return err
			}
		}
	}
	for _, f := range fmq.ctx.Fields {
		if !failedmessage.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fmq.path != nil {
		prev, err := fmq.path(ctx)
		if err != nil {
			return err
		}
		fmq.sql = prev
	}
	return nil
}

func (fmq *FailedMessageQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FailedMessage, error) {
	var (
		nodes = []*FailedMessage{}
		_spec = fmq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FailedMessage).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FailedMessage{config: fmq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(fmq.modifiers) > 0 {
		_spec.Modifiers = fmq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fmq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (fmq *FailedMessageQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fmq.querySpec()
	if len(fmq.modifiers) > 0 {
		_spec.Modifiers = fmq.modifiers
	}
	_spec.Node.Columns = fmq.ctx.Fields
	if len(fmq.ctx.Fields) > 0 {
		_spec.Unique = fmq.ctx.Unique != nil && *fmq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fmq.driver, _spec)
}

func (fmq *FailedMessageQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(failedmessage.Table, failedmessage.Columns, sqlgraph.NewFieldSpec(failedmessage.FieldID, field.TypeInt64))
	_spec.From = fmq.sql
	if unique := fmq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fmq.path != nil {
		_spec.Unique = true
	}
	if fields := fmq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, failedmessage.FieldID)
		for i := range fields {
			if fields[i] != failedmessage.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fmq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fmq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fmq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fmq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fmq *FailedMessageQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fmq.driver.Dialect())
	t1 := builder.Table(failedmessage.Table)
	columns := fmq.ctx.Fields
	if len(columns) == 0 {
		columns = failedmessage.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fmq.sql != nil {
		selector = fmq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fmq.ctx.Unique != nil && *fmq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range fmq.modifiers {
		m(selector)
	}
	for _, p := range fmq.predicates {
		p(selector)
	}
	for _, p := range fmq.order {
		p(selector)
	}
	if offset := fmq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fmq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (fmq *FailedMessageQuery) ForUpdate(opts ...sql.LockOption) *FailedMessageQuery {
	if fmq.driver.Dialect() == dialect.Postgres {
		fmq.Unique(false)
	}
	fmq.modifiers = append(fmq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return fmq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (fmq *FailedMessageQuery) ForShare(opts ...sql.LockOption) *FailedMessageQuery {
	if fmq.driver.Dialect() == dialect.Postgres {
		fmq.Unique(false)
	}
	fmq.modifiers = append(fmq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return fmq
}

// FailedMessageGroupBy is the group-by builder for FailedMessage entities.
type FailedMessageGroupBy struct {
	selector
	build *FailedMessageQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fmgb *FailedMessageGroupBy) Aggregate(fns ...AggregateFunc) *FailedMessageGroupBy {
	fmgb.fns = append(fmgb.fns, fns...)
	return fmgb
}

// Scan applies the selector query and scans the result into the given value.
func (fmgb *FailedMessageGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fmgb.build.ctx, "GroupBy")
	if err := fmgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FailedMessageQuery, *FailedMessageGroupBy](ctx, fmgb.build, fmgb, fmgb.build.inters, v)
}

func (fmgb *FailedMessageGroupBy) sqlScan(ctx context.Context, root *FailedMessageQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fmgb.fns))
	for _, fn := range fmgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fmgb.flds)+len(fmgb.fns))
		for _, f := range *fmgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fmgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fmgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FailedMessageSelect is the builder for selecting fields of FailedMessage entities.
type FailedMessageSelect struct {
	*FailedMessageQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fms *FailedMessageSelect) Aggregate(fns ...AggregateFunc) *FailedMessageSelect {
	fms.fns = append(fms.fns, fns...)
	return fms
}

// Scan applies the selector query and scans the result into the given value.
func (fms *FailedMessageSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fms.ctx, "Select")
	if err := fms.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FailedMessageQuery, *FailedMessageSelect](ctx, fms.FailedMessageQuery, fms, fms.inters, v)
}

func (fms *FailedMessageSelect) sqlScan(ctx context.Context, root *FailedMessageQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fms.fns))
	for _, fn := range fms.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fms.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fms.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
