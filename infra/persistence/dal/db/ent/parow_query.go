// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"purchase/infra/persistence/dal/db/ent/parow"
	"purchase/infra/persistence/dal/db/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PARowQuery is the builder for querying PARow entities.
type PARowQuery struct {
	config
	ctx        *QueryContext
	order      []parow.OrderOption
	inters     []Interceptor
	predicates []predicate.PARow
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the PARowQuery builder.
func (prq *PARowQuery) Where(ps ...predicate.PARow) *PARowQuery {
	prq.predicates = append(prq.predicates, ps...)
	return prq
}

// Limit the number of records to be returned by this query.
func (prq *PARowQuery) Limit(limit int) *PARowQuery {
	prq.ctx.Limit = &limit
	return prq
}

// Offset to start from.
func (prq *PARowQuery) Offset(offset int) *PARowQuery {
	prq.ctx.Offset = &offset
	return prq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (prq *PARowQuery) Unique(unique bool) *PARowQuery {
	prq.ctx.Unique = &unique
	return prq
}

// Order specifies how the records should be ordered.
func (prq *PARowQuery) Order(o ...parow.OrderOption) *PARowQuery {
	prq.order = append(prq.order, o...)
	return prq
}

// First returns the first PARow entity from the query.
// Returns a *NotFoundError when no PARow was found.
func (prq *PARowQuery) First(ctx context.Context) (*PARow, error) {
	nodes, err := prq.Limit(1).All(setContextOp(ctx, prq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{parow.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (prq *PARowQuery) FirstX(ctx context.Context) *PARow {
	node, err := prq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first PARow ID from the query.
// Returns a *NotFoundError when no PARow ID was found.
func (prq *PARowQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = prq.Limit(1).IDs(setContextOp(ctx, prq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{parow.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (prq *PARowQuery) FirstIDX(ctx context.Context) int64 {
	id, err := prq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single PARow entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one PARow entity is found.
// Returns a *NotFoundError when no PARow entities are found.
func (prq *PARowQuery) Only(ctx context.Context) (*PARow, error) {
	nodes, err := prq.Limit(2).All(setContextOp(ctx, prq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{parow.Label}
	default:
		return nil, &NotSingularError{parow.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (prq *PARowQuery) OnlyX(ctx context.Context) *PARow {
	node, err := prq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only PARow ID in the query.
// Returns a *NotSingularError when more than one PARow ID is found.
// Returns a *NotFoundError when no entities are found.
func (prq *PARowQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = prq.Limit(2).IDs(setContextOp(ctx, prq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{parow.Label}
	default:
		err = &NotSingularError{parow.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (prq *PARowQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := prq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of PARows.
func (prq *PARowQuery) All(ctx context.Context) ([]*PARow, error) {
	ctx = setContextOp(ctx, prq.ctx, "All")
	if err := prq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*PARow, *PARowQuery]()
	return withInterceptors[[]*PARow](ctx, prq, qr, prq.inters)
}

// AllX is like All, but panics if an error occurs.
func (prq *PARowQuery) AllX(ctx context.Context) []*PARow {
	nodes, err := prq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of PARow IDs.
func (prq *PARowQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if prq.ctx.Unique == nil && prq.path != nil {
		prq.Unique(true)
	}
	ctx = setContextOp(ctx, prq.ctx, "IDs")
	if err = prq.Select(parow.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (prq *PARowQuery) IDsX(ctx context.Context) []int64 {
	ids, err := prq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (prq *PARowQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, prq.ctx, "Count")
	if err := prq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, prq, querierCount[*PARowQuery](), prq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (prq *PARowQuery) CountX(ctx context.Context) int {
	count, err := prq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (prq *PARowQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, prq.ctx, "Exist")
	switch _, err := prq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (prq *PARowQuery) ExistX(ctx context.Context) bool {
	exist, err := prq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the PARowQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (prq *PARowQuery) Clone() *PARowQuery {
	if prq == nil {
		return nil
	}
	return &PARowQuery{
		config:     prq.config,
		ctx:        prq.ctx.Clone(),
		order:      append([]parow.OrderOption{}, prq.order...),
		inters:     append([]Interceptor{}, prq.inters...),
		predicates: append([]predicate.PARow{}, prq.predicates...),
		// clone intermediate query.
		sql:  prq.sql.Clone(),
		path: prq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		DocCode string `json:"doc_code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.PARow.Query().
//		GroupBy(parow.FieldDocCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (prq *PARowQuery) GroupBy(field string, fields ...string) *PARowGroupBy {
	prq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &PARowGroupBy{build: prq}
	grbuild.flds = &prq.ctx.Fields
	grbuild.label = parow.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		DocCode string `json:"doc_code,omitempty"`
//	}
//
//	client.PARow.Query().
//		Select(parow.FieldDocCode).
//		Scan(ctx, &v)
func (prq *PARowQuery) Select(fields ...string) *PARowSelect {
	prq.ctx.Fields = append(prq.ctx.Fields, fields...)
	sbuild := &PARowSelect{PARowQuery: prq}
	sbuild.label = parow.Label
	sbuild.flds, sbuild.scan = &prq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a PARowSelect configured with the given aggregations.
func (prq *PARowQuery) Aggregate(fns ...AggregateFunc) *PARowSelect {
	return prq.Select().Aggregate(fns...)
}

func (prq *PARowQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range prq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, prq); err != nil {
				return err
			}
		}
	}
	for _, f := range prq.ctx.Fields {
		if !parow.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if prq.path != nil {
		prev, err := prq.path(ctx)
		if err != nil {
			return err
		}
		prq.sql = prev
	}
	return nil
}

func (prq *PARowQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*PARow, error) {
	var (
		nodes = []*PARow{}
		_spec = prq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*PARow).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &PARow{config: prq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, prq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (prq *PARowQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := prq.querySpec()
	_spec.Node.Columns = prq.ctx.Fields
	if len(prq.ctx.Fields) > 0 {
		_spec.Unique = prq.ctx.Unique != nil && *prq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, prq.driver, _spec)
}

func (prq *PARowQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(parow.Table, parow.Columns, sqlgraph.NewFieldSpec(parow.FieldID, field.TypeInt64))
	_spec.From = prq.sql
	if unique := prq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if prq.path != nil {
		_spec.Unique = true
	}
	if fields := prq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, parow.FieldID)
		for i := range fields {
			if fields[i] != parow.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := prq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := prq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := prq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := prq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (prq *PARowQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(prq.driver.Dialect())
	t1 := builder.Table(parow.Table)
	columns := prq.ctx.Fields
	if len(columns) == 0 {
		columns = parow.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if prq.sql != nil {
		selector = prq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if prq.ctx.Unique != nil && *prq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range prq.predicates {
		p(selector)
	}
	for _, p := range prq.order {
		p(selector)
	}
	if offset := prq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := prq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// PARowGroupBy is the group-by builder for PARow entities.
type PARowGroupBy struct {
	selector
	build *PARowQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (prgb *PARowGroupBy) Aggregate(fns ...AggregateFunc) *PARowGroupBy {
	prgb.fns = append(prgb.fns, fns...)
	return prgb
}

// Scan applies the selector query and scans the result into the given value.
func (prgb *PARowGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prgb.build.ctx, "GroupBy")
	if err := prgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PARowQuery, *PARowGroupBy](ctx, prgb.build, prgb, prgb.build.inters, v)
}

func (prgb *PARowGroupBy) sqlScan(ctx context.Context, root *PARowQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(prgb.fns))
	for _, fn := range prgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*prgb.flds)+len(prgb.fns))
		for _, f := range *prgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*prgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// PARowSelect is the builder for selecting fields of PARow entities.
type PARowSelect struct {
	*PARowQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (prs *PARowSelect) Aggregate(fns ...AggregateFunc) *PARowSelect {
	prs.fns = append(prs.fns, fns...)
	return prs
}

// Scan applies the selector query and scans the result into the given value.
func (prs *PARowSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, prs.ctx, "Select")
	if err := prs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*PARowQuery, *PARowSelect](ctx, prs.PARowQuery, prs, prs.inters, v)
}

func (prs *PARowSelect) sqlScan(ctx context.Context, root *PARowQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(prs.fns))
	for _, fn := range prs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*prs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := prs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
